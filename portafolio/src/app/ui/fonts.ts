import { Meow_Script, IBM_Plex_Mono, PT_Mono, Prompt } from 'next/font/google';

export  const meowScript = Meow_Script({subsets:['latin'], weight: '400'});
export const ibmPlexMono = IBM_Plex_Mono({subsets:['latin'], weight: '500'});
export const ptMono = PT_Mono({subsets:['latin'], weight: '400'});
export const prompt = Prompt({subsets:['latin'], weight: '400'});